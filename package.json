{
  "name": "@timondev/snowflakes",
  "version": "0.0.1",
  "description": "library for generating unique snowflake IDs",
  "type": "module",
  "module": "src/snowflake.ts",
  "sideEffects": false,
  "scripts": {
    "build": "bun run utils/build.ts",
    "benchmark": "bun run utils/benchmark.ts"
  },
  "devDependencies": {
    "@mitata/counters": "^0.0.8",
    "@types/bun": "latest",
    "mitata": "^1.0.34"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}